services:
  bot:
    build: .
    image: jw-talks-planner:latest
    container_name: jw-talks-planner
    env_file: .env
    environment:
      DB_PATH: /data/talks.db
    volumes:
      - bot-data:/data
    restart: unless-stopped

volumes:
  bot-data:

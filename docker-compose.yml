services:
  postgres:
    image: postgres:16-alpine
    container_name: jw-talks-planner-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    mem_limit: 256m
    cpus: 1.0
    networks:
      - bot-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jw-talks-bot
    restart: always
    depends_on:
      postgres:
        condition: service_healthy 
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://${DB_USER}:${DB_PASSWORD}@jw-talks-planner-db:5432/${DB_NAME}?schema=public
      TELEGRAM_API_ROOT: ${TELEGRAM_API_ROOT}

    networks:
      - bot-network

volumes:
  postgres-data:

networks:
  bot-network:
    driver: bridge
